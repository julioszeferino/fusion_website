### Comandos
```bash
$ docker-compose build # criando as imagens
$ docker-compose up -d # subindo os containers
$ docker exec -it fusion1 python manage.py migrate # criando os bancos da aplicacao
$ docker compose down # removendo os containers
```

